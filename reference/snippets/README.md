# Reference snippets

Illustrative Python only. Not a package and not wired to any live system.

From this folder:

```bash
cd reference/snippets
python -c "from gbm_fair_p import gbm_mr_p; print(gbm_mr_p(97500, 97000, 0.65, 120000))"
```

If a snippet disagrees with `docs/`, trust the doc.
